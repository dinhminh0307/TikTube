package com.example.tiktube.backend.utils;

public class Enums {
    public enum UserType {
        CURRENT_USER, // Represents the currently logged-in user
        FOLLOWER,     // Represents a user who is following the current user
        OTHER         // Represents any other user
    }
}
